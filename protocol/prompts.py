# RAG 问答的 Prompt 模板
prompt_template = """请根据以下上下文信息，生成符合要求的题目。

上下文：
{context}

用户问题：{question}

请严格按照以下JSON格式返回一个或多个题目：
[
  {{
    "question": "题目内容",
    "options": ["选项A", "选项B", "选项C", "选项D"],
    "answer": "正确答案",
    "explanation": "题目解析",
    "difficulty": "难度级别"
  }}
]

要求：
1. 题目内容要基于提供的上下文信息
2. 选项数量为4个，用A、B、C、D表示
3. 答案填写正确选项的字母（如：A、B、C、D）
4. 解析要详细说明为什么选择这个答案
5. 难度级别分为：简单、中等、困难

请直接返回JSON格式，不要有其他文字说明。"""

# AI 生成类似题的 Prompt 模板
generate_similar_question_template = """你是一个专业的试题生成专家。请根据提供的原题和相关知识点，生成一道新的类似题目。

### 原题信息
大题干：{original_big_stem}
小题干：{original_stem}
选项：
{original_options}
答案：{original_answer}
解析：{original_analysis}
知识点：{knowledge_point}
难度：{difficulty}

### 知识库相关内容
{context}

### 用户需求
{user_requirement}

### 生成要求
1. **保持知识点一致**：新题目必须考查相同的知识点：{knowledge_point}
2. **保持难度相近**：新题目难度应为：{difficulty}
3. **题干必须与原题有明确差异**：至少要修改题干内容本身，不能只改选项、答案或解析
4. **基于知识库内容**：充分利用知识库中提供的相关内容来构建题目
5. **满足用户需求**：根据用户的具体要求调整题目
6. **确保质量**：题目应该严谨、准确、有区分度
7. **必须至少改变两项关键要素**：题目情境、题干问法、选项内容、案例数据、干扰项设计中至少改变两项
8. **禁止输出与原题相同或近乎相同的题目**：如果生成结果与原题重复，请重新生成，直到明显不同为止
9. **保持原题结构一致**：
   - 如果原题只有大题干而没有小题干，那么新题也必须只有大题干，小题干必须为空字符串
   - 如果原题同时有大题干和小题干，则新题也保持这个结构
10. **优先修改题干而不是解析**：解析只是说明，不能把“改了解析”当成“出了新题”

### 输出格式
请严格按照以下 JSON 格式返回生成的题目（只返回一道题）：

{{
  "big_stem": "新题目的大题干",
  "stem": "新题目的小题干，如果原题没有小题干则必须返回空字符串",
  "options": ["选项A", "选项B", "选项C", "选项D"],
  "answer": "正确答案字母（A/B/C/D）",
  "explanation": "详细的解析说明",
  "difficulty": "难度等级（与原题相同）",
  "knowledge_point": "{knowledge_point}"
}}

请直接返回 JSON 格式，不要包含任何其他说明文字或 markdown 标记。"""

# E-R 图生成的 Prompt 模板
generate_er_diagram_template = """你是一个数据库设计专家。请随机生成一个 E-R 图的结构化描述。

### 生成要求
1. **实体数量**：生成 2-3 个实体（Entity）
2. **实体属性**：每个实体应该有 1-4 个属性，其中一个必须是主键（标记为 isPrimaryKey: true）
3. **关系类型**：实体之间应该有关系，关系类型包括：
   - 一对一（1:1）
   - 一对多（1:N）
   - 多对多（M:N）
4. **关系属性**：每个关系可以有 0-4 个描述性属性（关系可以没有属性）
5. **属性总数控制**：请确保生成的所有实体属性和关系属性的总数符合要求
6. **主题多样性**：可以从以下领域随机选择：学校管理、电商系统、医院管理、图书馆、社交网络、在线教育等

### 输出格式
请严格按照以下 JSON 格式返回 E-R 图结构：

{{
  "entities": [
    {{
      "name": "实体名称",
      "attributes": [
        {{"name": "属性名", "type": "数据类型", "isPrimaryKey": true}},
        {{"name": "属性名", "type": "数据类型", "isPrimaryKey": false}},
        {{"name": "属性名", "type": "数据类型", "isPrimaryKey": false}}
      ]
    }}
  ],
  "relationships": [
    {{
      "name": "关系名称",
      "entity1": "实体1名称",
      "entity2": "实体2名称",
      "cardinality": "关系类型（1:1, 1:N, M:N）",
      "attributes": [
        {{"name": "属性名", "type": "数据类型"}},
        {{"name": "属性名", "type": "数据类型"}}
      ]
    }}
  ]
}}

### 示例
{{
  "entities": [
    {{
      "name": "学生",
      "attributes": [
        {{"name": "学号", "type": "VARCHAR(20)", "isPrimaryKey": true}},
        {{"name": "姓名", "type": "VARCHAR(50)", "isPrimaryKey": false}},
        {{"name": "年龄", "type": "INT", "isPrimaryKey": false}},
        {{"name": "专业", "type": "VARCHAR(50)", "isPrimaryKey": false}}
      ]
    }},
    {{
      "name": "课程",
      "attributes": [
        {{"name": "课程号", "type": "VARCHAR(20)", "isPrimaryKey": true}},
        {{"name": "课程名", "type": "VARCHAR(100)", "isPrimaryKey": false}},
        {{"name": "学分", "type": "INT", "isPrimaryKey": false}}
      ]
    }}
  ],
  "relationships": [
    {{
      "name": "选课",
      "entity1": "学生",
      "entity2": "课程",
      "cardinality": "M:N",
      "attributes": [
        {{"name": "选课时间", "type": "DATETIME"}},
        {{"name": "成绩", "type": "FLOAT"}},
        {{"name": "学期", "type": "VARCHAR(20)"}}
      ]
    }}
  ]
}}

请直接返回 JSON 格式，不要包含任何其他说明文字或 markdown 标记。确保生成的实体和关系在语义上是合理的。"""

# E-R图背景生成模板
generate_background_template = """你是一位数据库设计专家。请根据以下E-R图数据，生成一段约200字的背景描述，介绍系统的实体、属性以及它们之间的关系。这段文字将作为数据库设计题目的一部分。

E-R图数据：
{er_data}

要求：
1. 字数控制在200字左右。
2. 提供清晰的情景描述（如学校管理系统、图书馆管理系统、电商平台等）。
3. 介绍主要实体及其包含的属性。
4. 描述实体之间的联系。
5. 这是一个题目的背景介绍，不要包含"这是一道题目"之类的描述，直接描述系统背景。
5. 简要提及关键属性。
6. 语言简洁流畅，符合考试题目风格。
7. 不要使用第一人称，使用客观描述。

请直接返回题干内容，不要包含任何其他说明文字。"""

# 数据库物理设计生成模板
generate_physical_design_template = """你是一个数据库设计专家。请根据以下 E-R 图数据，生成一段数据库物理设计题目。
题目要求包含以下部分：
1. 为所有实体生成对应的数据库表结构描述。
2. 如果存在多对多（M:N）关系，必须额外生成对应的中间关系表。
3. 针对生成的这些表生成两部分题目：（1）完善数据库关系模式；（2）完善数据表设计填空。

E-R图数据：
{er_data}

请严格按照以下 JSON 格式返回生成内容：
{{
  "tables": [
    {{
      "name": "表名（英文）",
      "description": "表描述（中文）",
      "columns": [
        {{"name": "字段名", "desc": "字段描述", "type": "数据类型", "constraint": "属性限制（如PK, FK, NULL, NOT NULL）"}}
      ]
    }}
  ],
  "relation_questions": [
    {{
      "table_name": "表名",
      "relation_mode": "关系模式字符串（格式：表名(字段1, 字段2...)，其中需包含1-2个下划线挖空）",
      "pk": "主关键字答案",
      "fk": "外关键字答案（无则填无）",
      "answer": "挖空处的答案"
    }}
  ],
  "design_questions": [
    {{
      "question": "1）xxx表的xxx字段数据类型定义 ________ 。",
      "answer": "答案",
      "note": "该题必须与表结构展示区某个字段的数据类型挖空位置一致"
    }},
    {{
      "question": "2）在xxx表中为了加快按xxx查询的速度，可以在___________字段上建立索引。",
      "answer": "答案"
    }},
    {{
      "question": "3）请根据下方样例数据，填写xxx表中的空缺数据 _________ 。",
      "answer": "答案",
      "note": "该题答案必须对应下方样例数据表中的唯一挖空值"
    }},
    {{
      "question": "4）往数据表中输入数据时，应该最后输入 __________ 数据表的数据。",
      "answer": "答案（通常是包含外键的表）"
    }}
  ]
}}

要求：
1. 必须覆盖所有实体表；若存在 M:N 关系，还必须包含对应的中间表。每个表至少 3-4 个字段。
2. 关系模式问题中，挖空应涉及属性、主键或外键。
3. 设计填空题必须基于生成的表结构和样例数据。
4. 第1小题必须与表结构展示区某个字段类型的挖空位置保持一致。
5. 第3小题必须优先针对多对多关系拆分后单独表所引用的主表主键样例数据挖空提问；若不存在 M:N 拆分表，才允许回退到其他主外键相关样例数据。
6. 直接返回 JSON，不要包含 Markdown 标记。
"""
